# Database Configuration
# MySQL Database (Primary)
DATABASE_URL="mysql://borrands_user:borrands@12@127.0.0.1:3306/borrands_webapp"

# Development MySQL
# DATABASE_URL="mysql://root:password@localhost:3306/borrands_dev"

# Production MySQL
# DATABASE_URL="mysql://username:password@your-mysql-host:3306/borrands"

# Individual MySQL Connection Variables (Alternative)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=borrands_webapp
DB_USERNAME=borrands_user
DB_PASSWORD=borrands@12

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Twilio WhatsApp Configuration
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+14155238886

# Paystack Payment Gateway
PAYSTACK_SECRET_KEY=your_paystack_secret_key
PAYSTACK_PUBLIC_KEY=your_paystack_public_key

# Database Performance Settings (Optional)
# DB_MAX_POOL_SIZE=20
# DB_MIN_POOL_SIZE=5
# DB_MAX_IDLE_TIME_MS=30000

# Email Configuration (Required for email OTP verification)
# You can use either MAIL_* or SMTP_* variable names
MAIL_HOST=mail.borrands.com.ng
MAIL_PORT=465
MAIL_USERNAME=support@borrands.com.ng
MAIL_PASSWORD=your_email_password_here
MAIL_FROM_NAME=Borrands
MAIL_FROM_ADDRESS=noreply@borrands.com.ng

# Alternative SMTP variable names (if you prefer)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Optional: Cloudinary (for image uploads)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# Optional: Redis (for caching)
# REDIS_URL=redis://localhost:6379

# Optional: Sentry (for error tracking)
# SENTRY_DSN=your_sentry_dsn

# Optional: Google Analytics
# GA_TRACKING_ID=your_ga_tracking_id

# Optional: Facebook Pixel
# FB_PIXEL_ID=your_fb_pixel_id
